# Pinned, compatible project dependencies for Python 3.11

# Core ML / CV
numpy==1.23.5
tensorflow==2.12.0
h5py==3.15.1
opencv-python==4.7.0.72
scikit-learn==1.3.2
scipy==1.15.3
matplotlib==3.10.7

# Utilities / Web / I/O
pillow==12.0.0
requests==2.32.5
fastapi==0.121.2
uvicorn[standard]==0.38.0
playsound==1.2.2

# Native / build-time helpers
dlib==20.0.0
cmake==4.1.2

# Notes:
# - These versions were verified together on Python 3.11 in the `drowzy` virtualenv.
# - TensorFlow 2.12 is incompatible with NumPy >=1.24; keep `numpy==1.23.5` to avoid errors.
# - On Windows, building `dlib` may require Visual C++ Build Tools. If you face build errors,
#   either install the build tools, use a prebuilt wheel, or install via conda (`conda install -c conda-forge dlib`).